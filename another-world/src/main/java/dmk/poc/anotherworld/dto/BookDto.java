package dmk.poc.anotherworld.dto;

public record BookDto(String isbn, String title, String author, String publisher, String timestamp) {
    // No additional methods or fields are needed for this record
}
